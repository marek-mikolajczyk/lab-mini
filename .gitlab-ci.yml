install_webserver:
    stage: build
    script:
        - dnf install -y httpd
        - firewall-cmd --add-service=httpd --permanent
        - firewall-cmd --reload
        - systemctl enable --now httpd
deploy_www_content:
    stage: deploy
    script:
        - echo "pipeline result v001" > /var/www/html/index.html